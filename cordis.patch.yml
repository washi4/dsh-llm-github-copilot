# dsh-llm-github-copilot bundle patch.
# Declares the GitHub Copilot LLM adapter entry. Applied as one layer in the
# profile bundle stack; the user's cordis.patch.yml may override config by id.
- insert:
    - id: llm-github-copilot
      name: '@washi4/dsh-llm-github-copilot'
